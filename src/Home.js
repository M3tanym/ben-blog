import React from 'react';
import {Container, Typography} from "@mui/material";

const Home = (props) => {
    return (
        <Container>
            <title>Home</title>
            <Typography variant="h5">
                Home
            </Typography>
        </Container>
    );
};

export default Home;
